(define (domain windfarm-1)
    (:requirements :strips :typing)

    (:types

    )

    (:predicates

    )

    (:action action_name
        :parameters ()
        :precondition (and)
        :effect (and)
    )

)